apiVersion: v1
kind: Service
metadata:
  name: voice-agent
  namespace: voice-agent
spec:
  type: ClusterIP
  selector:
    app: voice-agent
  ports:
    - name: http
      port: 80
      targetPort: 3000
