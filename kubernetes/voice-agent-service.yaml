apiVersion: v1
kind: Service
metadata:
  name: voice-agent-svc
  namespace: voice-agent
spec:
  selector:
    app: voice-agent
    role: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
